% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soil_water_diffusivity.R
\name{soil_water_diffusivity}
\alias{soil_water_diffusivity}
\title{Compute soil water diffusivity (Van Genuchten / Mualem)}
\usage{
soil_water_diffusivity(data, ks, theta_r, theta_s, alpha, n, m, h, tau = 0.5)
}
\arguments{
\item{data}{A data frame or tibble. Passed as the first argument so the
function is compatible with \verb{|>} and \verb{\%>\%} pipes.}

\item{ks}{Saturated hydraulic conductivity (e.g. cm/day). Either a bare
column name from \code{data} or a scalar numeric value. Must be strictly
positive.}

\item{theta_r}{Residual water content (m³/m³). Either a bare column name
or a scalar numeric value.}

\item{theta_s}{Saturated water content (m³/m³). Either a bare column name
or a scalar numeric value.}

\item{alpha}{Van Genuchten shape parameter (1/cm or 1/m, matching units of
\code{h}). Either a bare column name or a scalar numeric value. Must be
strictly positive.}

\item{n}{Van Genuchten shape parameter (dimensionless). Must be > 1 for the
Mualem model. Either a bare column name or a scalar numeric value.}

\item{m}{Van Genuchten shape parameter (dimensionless). Typically \code{1 - 1/n}.
Must satisfy \verb{0 < m < 1}. Either a bare column name or a scalar numeric
value. Used only in the K(h) formula; C(h) derives m from n internally.}

\item{h}{Matric potential / pressure head (cm or m, must match units of
\code{alpha}). Either a bare column name or a scalar numeric value. Sign
convention: absolute value is used internally.}

\item{tau}{Tortuosity/connectivity parameter (dimensionless). Defaults to
\code{0.5} (Mualem 1976). Must satisfy \code{tau > -2}. Either a bare column name
or a scalar numeric value.}
}
\value{
The input \code{data} as a tibble with an additional column \code{.D}
containing soil water diffusivity in units of length²/time (e.g.
cm²/day when \code{ks} is in cm/day and \code{h} in cm).
}
\description{
Calculates soil water diffusivity D(h) at given matric potential values:
\deqn{D(h) = \frac{K(h)}{C(h)}}
}
\details{
where K(h) is the Mualem-Van Genuchten hydraulic conductivity and C(h) is
the Van Genuchten soil water capacity (−dθ/dh). D(h) has units of length²
per time (e.g. cm²/day when \code{ks} is in cm/day and \code{h} in cm).

D(h) appears in the diffusive form of the Richards equation:
\deqn{\frac{\partial \theta}{\partial t} =
  \nabla \cdot \left(D(\theta) \nabla \theta\right) - \frac{\partial K}{\partial z}}

and is used in analytical solutions for infiltration and redistribution.
}
\note{
D(h) is undefined at saturation (h = 0) because C(0) = 0.
The function returns \code{Inf} at h = 0 unless K(0) is also zero, in which
case R returns \code{NaN} via \code{0/0}. Handle h = 0 rows in advance if needed.
}
\examples{
library(tibble)

# Exclude h = 0 (D is undefined at saturation)
soil <- tibble(h = c(1, 10, 100, 1000, 15000))

soil_water_diffusivity(
  soil,
  ks      = 10,
  theta_r = 0.05,
  theta_s = 0.45,
  alpha   = 0.02,
  n       = 1.5,
  m       = 1 - 1/1.5,
  h       = h
)

}
\references{
Mualem, Y. (1976). A new model for predicting the hydraulic conductivity of
unsaturated porous media. \emph{Water Resources Research}, 12(3), 513–522.
\url{https://doi.org/10.1029/WR012i003p00513}

Van Genuchten, M. Th. (1980). A closed-form equation for predicting the
hydraulic conductivity of unsaturated soils. \emph{Soil Science Society of
America Journal}, 44(5), 892–898.
\url{https://doi.org/10.2136/sssaj1980.03615995004400050002x}
}
\seealso{
\code{\link[=hydraulic_conductivity]{hydraulic_conductivity()}} for K(h),
\code{\link[=soil_water_capacity]{soil_water_capacity()}} for C(h), \code{\link[=swrc_van_genuchten]{swrc_van_genuchten()}} for θ(h).
}

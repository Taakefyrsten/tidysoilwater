% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soil_water_capacity.R
\name{soil_water_capacity}
\alias{soil_water_capacity}
\title{Compute soil water capacity (Van Genuchten)}
\usage{
soil_water_capacity(data, theta_r, theta_s, alpha, n, h)
}
\arguments{
\item{data}{A data frame or tibble. Passed as the first argument so the
function is compatible with \verb{|>} and \verb{\%>\%} pipes.}

\item{theta_r}{Residual water content (m³/m³). Either a bare column name
from \code{data} or a scalar numeric value.}

\item{theta_s}{Saturated water content (m³/m³). Either a bare column name
from \code{data} or a scalar numeric value.}

\item{alpha}{Van Genuchten shape parameter (1/cm or 1/m, matching units of
\code{h}). Either a bare column name or a scalar numeric value. Must be
strictly positive.}

\item{n}{Van Genuchten shape parameter (dimensionless). Must be > 1 for the
Mualem model. Either a bare column name or a scalar numeric value.}

\item{h}{Matric potential / pressure head (cm or m, must match units of
\code{alpha}). Either a bare column name or a scalar numeric value. Sign
convention: absolute value is used internally.}
}
\value{
The input \code{data} as a tibble with an additional column \code{.C}
containing soil water capacity in units of 1/cm (or 1/m, matching
the units of \code{h} and \code{alpha}). At h = 0, \code{.C} is 0.
}
\description{
Calculates the soil water capacity C(h) = −dθ/dh at given matric potential
values using the analytical derivative of the Van Genuchten (1980) model
with the Mualem constraint (m = 1 − 1/n).
}
\details{
The soil water capacity is the slope of the soil water retention curve:
\deqn{C(h) = -\frac{d\theta}{dh} =
  \frac{(\theta_s - \theta_r) \cdot m \cdot n \cdot \alpha^n \cdot |h|^{n-1}}
       {(1 + (\alpha |h|)^n)^{m+1}}}

C(h) is zero at saturation (h = 0) and at very high suction (h → ∞), and
reaches a maximum at the inflection point of the retention curve.

C(h) is needed for computing soil water diffusivity D(h) = K(h)/C(h) and
appears in the Richards equation for unsaturated flow.
}
\examples{
library(tibble)

soil <- tibble(h = c(0, 10, 100, 1000, 15000))

soil_water_capacity(soil, theta_r = 0.05, theta_s = 0.45,
                    alpha = 0.02, n = 1.5, h = h)

# Pipeline with swrc_van_genuchten()
soil |>
  swrc_van_genuchten(theta_r = 0.05, theta_s = 0.45,
                     alpha = 0.02, n = 1.5, h = h) |>
  soil_water_capacity(theta_r = 0.05, theta_s = 0.45,
                      alpha = 0.02, n = 1.5, h = h)

}
\references{
Van Genuchten, M. Th. (1980). A closed-form equation for predicting the
hydraulic conductivity of unsaturated soils. \emph{Soil Science Society of
America Journal}, 44(5), 892–898.
\url{https://doi.org/10.2136/sssaj1980.03615995004400050002x}
}
\seealso{
\code{\link[=swrc_van_genuchten]{swrc_van_genuchten()}} for θ(h), \code{\link[=soil_water_diffusivity]{soil_water_diffusivity()}} for
D(h) = K(h)/C(h), \code{\link[=hydraulic_conductivity]{hydraulic_conductivity()}} for K(h).
}

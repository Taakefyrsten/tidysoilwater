% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/van_genuchten.R
\name{swrc_van_genuchten}
\alias{swrc_van_genuchten}
\title{Compute the Van Genuchten soil water retention curve}
\usage{
swrc_van_genuchten(data, theta_r, theta_s, alpha, n, h)
}
\arguments{
\item{data}{A data frame or tibble. Passed as the first argument so the
function is compatible with \verb{|>} and \verb{\%>\%} pipes.}

\item{theta_r}{Residual water content (m³/m³). Either a bare column name
from \code{data} or a scalar numeric value.}

\item{theta_s}{Saturated water content (m³/m³). Either a bare column name
from \code{data} or a scalar numeric value.}

\item{alpha}{Van Genuchten shape parameter (1/cm or 1/m, matching units of
\code{h}). Either a bare column name or a scalar numeric value. Must be
strictly positive.}

\item{n}{Van Genuchten shape parameter (dimensionless). Must be > 1 for the
Mualem model. Either a bare column name or a scalar numeric value.}

\item{h}{Matric potential / pressure head (cm or m, must match units of
\code{alpha}). Either a bare column name or a scalar numeric value. Sign
convention: positive values are treated as suction (absolute value is
used internally).}
}
\value{
The input \code{data} as a tibble with an additional column \code{.theta}
containing the computed volumetric water content (m³/m³).
}
\description{
Calculates volumetric soil water content at given matric potential values
using the Van Genuchten (1980) model with the Mualem (1976) constraint
(\code{m = 1 - 1/n}).
}
\details{
The model is:
\deqn{\theta(h) = \theta_r + \frac{\theta_s - \theta_r}{(1 + (\alpha |h|)^n)^m}}

where \code{m = 1 - 1/n}.
}
\examples{
library(tibble)

soil <- tibble(
  h     = c(0, 10, 100, 1000, 15000),
  alpha = 0.02,
  n     = 1.5
)

# Using bare column names for alpha and n, scalars for theta_r / theta_s
swrc_van_genuchten(soil, theta_r = 0.05, theta_s = 0.45, alpha = alpha,
                  n = n, h = h)

# All scalar values
swrc_van_genuchten(soil, theta_r = 0.05, theta_s = 0.45, alpha = 0.02,
                  n = 1.5, h = h)

}
\references{
Van Genuchten, M. Th. (1980). A closed-form equation for predicting the
hydraulic conductivity of unsaturated soils. \emph{Soil Science Society of
America Journal}, 44(5), 892–898.
\url{https://doi.org/10.2136/sssaj1980.03615995004400050002x}
}

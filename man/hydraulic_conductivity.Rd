% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hydraulic_conductivity.R
\name{hydraulic_conductivity}
\alias{hydraulic_conductivity}
\title{Compute unsaturated hydraulic conductivity (Mualem-Van Genuchten)}
\usage{
hydraulic_conductivity(data, ks, alpha, n, m, h, tau = 0.5)
}
\arguments{
\item{data}{A data frame or tibble. Passed as the first argument so the
function is compatible with \verb{|>} and \verb{\%>\%} pipes.}

\item{ks}{Saturated hydraulic conductivity (any consistent unit, e.g.
cm/day). Either a bare column name from \code{data} or a scalar numeric value.
Must be strictly positive.}

\item{alpha}{Van Genuchten shape parameter (1/cm or 1/m). Either a bare
column name or a scalar numeric value. Must be strictly positive.}

\item{n}{Van Genuchten shape parameter (dimensionless). Either a bare
column name or a scalar numeric value. Must be strictly positive.}

\item{m}{Van Genuchten shape parameter (dimensionless). Typically set to
\code{1 - 1/n}, but can be specified independently. Must satisfy \verb{0 < m < 1}.
Either a bare column name or a scalar numeric value.}

\item{h}{Matric potential / pressure head. Either a bare column name or a
scalar numeric value. Sign convention: absolute value is used internally.}

\item{tau}{Tortuosity/connectivity parameter (dimensionless). Defaults to
\code{0.5} (Mualem 1976). Must satisfy \code{tau > -2}. Either a bare column name
or a scalar numeric value.}
}
\value{
The input \code{data} as a tibble with an additional column \code{.K}
containing the computed hydraulic conductivity in the same units as \code{ks}.
}
\description{
Calculates unsaturated hydraulic conductivity at given matric potential
values using the Mualem (1976) - Van Genuchten (1980) model.
}
\details{
The effective saturation is first computed as:
\deqn{S_e(h) = \frac{1}{(1 + (\alpha |h|)^n)^m}}

Then hydraulic conductivity is given by:
\deqn{K(h) = K_s \cdot S_e^{\tau} \cdot \left(1 - \left(1 - S_e^{1/m}\right)^m\right)^2}

The tortuosity/connectivity parameter \code{tau} (τ) defaults to \code{0.5}, which is
the value proposed by Mualem (1976). Soilhypfit and other tools treat it as
a free parameter; values between −2 and 2 are physically plausible.
}
\examples{
library(tibble)

soil <- tibble(h = c(0, 10, 100, 1000, 15000))

# Default tau = 0.5 (Mualem)
hydraulic_conductivity(
  soil,
  ks    = 10,
  alpha = 0.02,
  n     = 1.5,
  m     = 1 - 1/1.5,
  h     = h
)

# Custom tortuosity parameter
hydraulic_conductivity(
  soil,
  ks    = 10,
  alpha = 0.02,
  n     = 1.5,
  m     = 1 - 1/1.5,
  h     = h,
  tau   = 1.0
)

}
\references{
Mualem, Y. (1976). A new model for predicting the hydraulic conductivity of
unsaturated porous media. \emph{Water Resources Research}, 12(3), 513–522.
\url{https://doi.org/10.1029/WR012i003p00513}

Van Genuchten, M. Th. (1980). A closed-form equation for predicting the
hydraulic conductivity of unsaturated soils. \emph{Soil Science Society of
America Journal}, 44(5), 892–898.
\url{https://doi.org/10.2136/sssaj1980.03615995004400050002x}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_swrc_hcc.R
\name{fit_swrc_hcc}
\alias{fit_swrc_hcc}
\title{Jointly fit Van Genuchten SWRC and hydraulic conductivity parameters}
\usage{
fit_swrc_hcc(
  data,
  theta_col,
  K_col,
  h_col,
  lower = NULL,
  upper = NULL,
  fixed = NULL,
  wrc_weight = 1,
  hcc_weight = 1,
  workers = 1L
)
}
\arguments{
\item{data}{A data frame or tibble, optionally grouped with
\code{\link[dplyr:group_by]{dplyr::group_by()}}.}

\item{theta_col}{Bare column name of observed volumetric water content
(m³/m³). Rows where this is \code{NA} are excluded from the WRC component.}

\item{K_col}{Bare column name of observed hydraulic conductivity. Rows
where this is \code{NA} are excluded from the HCC component. Must be positive;
the log₁₀ transformation is applied internally.}

\item{h_col}{Bare column name of matric potential / pressure head.
Absolute values are used internally.}

\item{lower}{Named numeric vector of lower parameter bounds. Parameter
names: \code{"theta_r"}, \code{"theta_s"}, \code{"alpha"}, \code{"n"}, \code{"Ks"}, \code{"tau"}.}

\item{upper}{Named numeric vector of upper parameter bounds.}

\item{fixed}{Named numeric vector of parameters to hold fixed. E.g.
\code{fixed = c(tau = 0.5)} (the default behaviour if \code{tau} is not supplied).}

\item{wrc_weight}{Positive scalar weighting the WRC residual component in
the joint objective. Defaults to \code{1}.}

\item{hcc_weight}{Positive scalar weighting the HCC (log K) residual
component. Defaults to \code{1}.}

\item{workers}{Number of parallel workers. Defaults to \code{1}.}
}
\value{
An object of class \code{fit_swrc_hcc} (a tibble subclass) with one row
per group containing:
\itemize{
\item Group keys (if grouped)
\item \code{theta_r}, \code{theta_s}, \code{alpha}, \code{n}, \code{Ks}, \code{tau}: fitted values
\item \verb{std_error_*} for each free parameter (approximate, from numerical Hessian)
\item \code{convergence}: \code{TRUE} if \code{optim()} returned code 0
}
}
\description{
Fits the Van Genuchten (1980) soil water retention curve and
Mualem-Van Genuchten hydraulic conductivity function simultaneously to
observed (h, θ) and (h, K) data. This joint fit shares the common
structural parameters (θ_r, θ_s, α, n) between both models, typically
improving identifiability compared to fitting them separately.
}
\details{
The objective function minimises the combined weighted residual:
\deqn{RSS = w_\theta \sum ({\theta_{obs}} - \theta_{pred})^2
            + w_K \sum (\log_{10}K_{obs} - \log_{10}K_{pred})^2}

K residuals are on the log₁₀ scale because K spans many orders of magnitude;
this prevents high-K observations from dominating the fit. The \code{wrc_weight}
and \code{hcc_weight} scalars allow the user to balance the two components.

Missing values (\code{NA}) in \code{theta_col} or \code{K_col} are silently dropped from
the respective component, so WRC and HCC measurements need not be at the
same h values and can be in the same data frame with NAs where data is
absent.
\subsection{Parameters estimated}{\tabular{ll}{
   Parameter \tab Meaning \cr
   \code{theta_r} \tab Residual water content (m³/m³) \cr
   \code{theta_s} \tab Saturated water content (m³/m³) \cr
   \code{alpha} \tab Van Genuchten shape (1/cm or 1/m) \cr
   \code{n} \tab Van Genuchten shape (dimensionless; > 1) \cr
   \code{Ks} \tab Saturated hydraulic conductivity (units of K_col) \cr
   \code{tau} \tab Mualem tortuosity (dimensionless; fixed at 0.5 by default) \cr
}

}
}
\examples{
library(tibble)

# Synthetic joint dataset — WRC and K at the same h values
joint <- tibble(
  h     = c(0, 10, 100, 500, 1000, 5000, 15000),
  theta = c(0.44, 0.40, 0.32, 0.25, 0.20, 0.12, 0.08),
  K     = c(25, 8.5, 0.9, 0.08, 0.012, 4e-4, 3e-5)
)

fit_swrc_hcc(joint, theta_col = theta, K_col = K, h_col = h)

# Fix tau at Mualem value; estimate the remaining 5 parameters
fit_swrc_hcc(joint, theta_col = theta, K_col = K, h_col = h,
             fixed = c(tau = 0.5))

# Mixed h values: WRC and HCC measured at different points
mixed <- tibble(
  h     = c(0, 10, 100, 500, 1000, 5000,  10, 100, 1000),
  theta = c(0.44, 0.40, 0.32, 0.25, 0.20, 0.12, NA, NA, NA),
  K     = c(NA, NA, NA, NA, NA, NA, 8.5, 0.9, 0.012)
)

fit_swrc_hcc(mixed, theta_col = theta, K_col = K, h_col = h)

}
\references{
Van Genuchten, M. Th. (1980). A closed-form equation for predicting the
hydraulic conductivity of unsaturated soils. \emph{Soil Science Society of
America Journal}, 44(5), 892–898.
\url{https://doi.org/10.2136/sssaj1980.03615995004400050002x}

Mualem, Y. (1976). A new model for predicting the hydraulic conductivity of
unsaturated porous media. \emph{Water Resources Research}, 12(3), 513–522.
\url{https://doi.org/10.1029/WR012i003p00513}
}
\seealso{
\code{\link[=fit_swrc]{fit_swrc()}}, \code{\link[=hydraulic_conductivity]{hydraulic_conductivity()}}, \code{\link[=swrc_van_genuchten]{swrc_van_genuchten()}}
}
